# Connects to your ak_admin database
spring.datasource.url=jdbc:mysql://localhost:3306/ak_admin?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=July$157

# Tells Spring Boot to run our table creation script on startup
spring.sql.init.mode=always

# Server Port
server.port=5000

# Gemini AI Configuration
gemini.api.key=${GEMINI_API_KEY}
google.gemini.url=https://generativelanguage.googleapis.com/v1beta/models/gemini-3.6-flash:generateContent?key=${GEMINI_API_KEY}

spring.mail.host=sandbox.smtp.mailtrap.io
spring.mail.port=2525
spring.mail.username=02f4b15b98b7f2
spring.mail.password=99be6a836ba11a
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=truels.enable=true